SOLUCIONES PERSONALES:

Tarea 1:

fun main() {
    val segmento = Event.Segmentation.NIGHT
    
    val evento: Event = Event(
        "Estudiar Kotlin",
        "Comprometerse a estudiar Kotlin al menos 15 minutos al día.",
        segmento,
        15
    )
    
    println(evento.toString())
}


data class Event(
    val title: String, 
    val description: String?,
    val daysegmentation: Segmentation,
    val duration: Int
) {
    enum class Segmentation {
        MORNING, EVENING, NIGHT
    }
}

Tarea 2:

fun main() {
    val segmento = Event.Segmentation.NIGHT
    
    val evento: Event = Event(
        "Estudiar Kotlin",
        "Comprometerse a estudiar Kotlin al menos 15 minutos al día.",
        segmento,
        15
    )
    
    println(evento.toString())
}


data class Event(
    val title: String, 
    val description: String?,
    val daysegmentation: Segmentation,
    val duration: Int
) {
    enum class Segmentation {
        MORNING, EVENING, NIGHT
    }
}

Tarea 3:

fun main() {
    
    val event1 = Event(title = "Wake up", description = "Time to get up", daypart = Daypart.MORNING, durationInMinutes = 0)
	val event2 = Event(title = "Eat breakfast", daypart = Daypart.MORNING, durationInMinutes = 15)
	val event3 = Event(title = "Learn about Kotlin", daypart = Daypart.AFTERNOON, durationInMinutes = 30)
	val event4 = Event(title = "Practice Compose", daypart = Daypart.AFTERNOON, durationInMinutes = 60)
	val event5 = Event(title = "Watch latest DevBytes video", daypart = Daypart.AFTERNOON, durationInMinutes = 10)
	val event6 = Event(title = "Check out latest Android Jetpack library", daypart = Daypart.EVENING, durationInMinutes = 45)
    
    val listaEventos = mutableListOf<Event>(event1, event2, event3, event4, event5, event6)
    println(listaEventos)
}

enum class Daypart {
    MORNING,
    AFTERNOON,
    EVENING,
}

data class Event(
    val title: String,
    val description: String? = null,
    val daypart: Daypart,
    val durationInMinutes: Int,
) 


Tarea 4:

fun main() {
    
    val event1 = Event(title = "Wake up", description = "Time to get up", daypart = Daypart.MORNING, durationInMinutes = 0)
	val event2 = Event(title = "Eat breakfast", daypart = Daypart.MORNING, durationInMinutes = 15)
	val event3 = Event(title = "Learn about Kotlin", daypart = Daypart.AFTERNOON, durationInMinutes = 30)
	val event4 = Event(title = "Practice Compose", daypart = Daypart.AFTERNOON, durationInMinutes = 60)
	val event5 = Event(title = "Watch latest DevBytes video", daypart = Daypart.AFTERNOON, durationInMinutes = 10)
	val event6 = Event(title = "Check out latest Android Jetpack library", daypart = Daypart.EVENING, durationInMinutes = 45)
    
    val listaEventos = mutableListOf<Event>(event1, event2, event3, event4, event5, event6)
    
    val eventosPequeños =  listaEventos.filter { it.durationInMinutes<=60}
    
    println("Tientes ${eventosPequeños.size} eventos de menos de 60 minutos.")
}

enum class Daypart {
    MORNING,
    AFTERNOON,
    EVENING,
}

data class Event(
    val title: String,
    val description: String? = null,
    val daypart: Daypart,
    val durationInMinutes: Int,
) 

Tarea 5:

fun main() {
    
    val event1 = Event(title = "Wake up", description = "Time to get up", daypart = Daypart.MORNING, durationInMinutes = 0)
	val event2 = Event(title = "Eat breakfast", daypart = Daypart.MORNING, durationInMinutes = 15)
	val event3 = Event(title = "Learn about Kotlin", daypart = Daypart.AFTERNOON, durationInMinutes = 30)
	val event4 = Event(title = "Practice Compose", daypart = Daypart.AFTERNOON, durationInMinutes = 60)
	val event5 = Event(title = "Watch latest DevBytes video", daypart = Daypart.AFTERNOON, durationInMinutes = 10)
	val event6 = Event(title = "Check out latest Android Jetpack library", daypart = Daypart.EVENING, durationInMinutes = 45)
    
    val listaEventos = mutableListOf<Event>(event1, event2, event3, event4, event5, event6)
    
    val eventosPequeños =  listaEventos.filter { it.durationInMinutes<=60}
    
    val eventosOrdenados = listaEventos.groupBy { it.daypart } 
    
    eventosOrdenados.forEach { (daypart, events) ->
    println("Para: $daypart hay ${listaEventos.size} eventos")
}

}

enum class Daypart {
    MORNING,
    AFTERNOON,
    EVENING,
}

data class Event(
    val title: String,
    val description: String? = null,
    val daypart: Daypart,
    val durationInMinutes: Int,
) 

Tarea 6:

fun main() {
    val event1 = Event(title = "Wake up", description = "Time to get up", daypart = Daypart.MORNING, durationInMinutes = 0)
    val event2 = Event(title = "Eat breakfast", daypart = Daypart.MORNING, durationInMinutes = 15)
    val event3 = Event(title = "Learn about Kotlin", daypart = Daypart.AFTERNOON, durationInMinutes = 30)
    val event4 = Event(title = "Practice Compose", daypart = Daypart.AFTERNOON, durationInMinutes = 60)
    val event5 = Event(title = "Watch latest DevBytes video", daypart = Daypart.AFTERNOON, durationInMinutes = 10)
    val event6 = Event(title = "Check out latest Android Jetpack library", daypart = Daypart.EVENING, durationInMinutes = 45)

    val listaEventos = mutableListOf<Event>(event1, event2, event3, event4, event5, event6)
    val eventosPequeños = listaEventos.filter { it.durationInMinutes <= 60 }
    val eventosOrdenados = listaEventos.groupBy { it.daypart }

    println("Last event of the day: ${listaEventos.last().title}")
}

enum class Daypart {
    MORNING, AFTERNOON, EVENING,
}

data class Event(
    val title: String,
    val description: String? = null,
    val daypart: Daypart,
    val durationInMinutes: Int,
)

Tarea 7:

fun main() {
    val event1 = Event(title = "Wake up", description = "Time to get up", daypart = Daypart.MORNING, durationInMinutes = 0)
    val event2 = Event(title = "Eat breakfast", daypart = Daypart.MORNING, durationInMinutes = 15)
    val event3 = Event(title = "Learn about Kotlin", daypart = Daypart.AFTERNOON, durationInMinutes = 30)
    val event4 = Event(title = "Practice Compose", daypart = Daypart.AFTERNOON, durationInMinutes = 60)
    val event5 = Event(title = "Watch latest DevBytes video", daypart = Daypart.AFTERNOON, durationInMinutes = 10)
    val event6 = Event(title = "Check out latest Android Jetpack library", daypart = Daypart.EVENING, durationInMinutes = 45)

    val listaEventos = mutableListOf<Event>(event1, event2, event3, event4, event5, event6)
    val eventosPequeños = listaEventos.filter { it.durationInMinutes <= 60 }
    val eventosOrdenados = listaEventos.groupBy { it.daypart }
    
    println("Duration of first event of the day: ${listaEventos[0].durationOfEvent}")
}

enum class Daypart {
    MORNING, AFTERNOON, EVENING,
}

data class Event(
    val title: String,
    val description: String? = null,
    val daypart: Daypart,
    val durationInMinutes: Int,
)

val Event.durationOfEvent: String
    get() = if (this.durationInMinutes < 60) {
        "short"
    } else {
        "long"
    }
